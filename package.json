{
  "name": "vue-node-env",
  "version": "0.1.0",
  "description": "A simple vue plugin to determine node environment",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": "git@github.com:Masa-Shin/vue-node-env.git",
  "author": "Masa-Shin",
  "license": "MIT",
  "private": false,
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc -p .",
    "prepublish": "yarn build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -p . --watch"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "typescript": "^4.2.3"
  }
}
